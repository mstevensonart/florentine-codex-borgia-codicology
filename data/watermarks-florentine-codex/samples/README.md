# Sample images

A small set of web-sized, illustrative scans (compressed JPG/PNG) go here for
documentation purposes -- e.g. one example per watermark type, a couple of
representative notebook pages. These are NOT the archival scans.

Full-resolution TIFF scans are excluded from this repo (see root `.gitignore`)
and are deposited on Zenodo alongside the dataset release instead.
